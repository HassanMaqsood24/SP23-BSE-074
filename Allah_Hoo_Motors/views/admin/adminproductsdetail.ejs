<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= product.title %> • Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
  <style>
    body {
      background: url('https://www.loucampaio.com/wp-content/plugins/admin-custom-login/images/3d-background.jpg') no-repeat center center fixed;
      background-size: cover;
      font-family: 'Segoe UI', sans-serif;
      padding: 40px 20px;
      color: #fff;
    }

    .detail-wrapper {
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);
      padding: 40px;
      max-width: 1000px;
      margin: auto;
      border-radius: 20px;
      color: #000;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
    }

    .product-title {
      font-size: 32px;
      font-weight: 700;
      margin-bottom: 30px;
      text-align: center;
    }

    .info-box {
      margin-bottom: 15px;
    }

    .info-label {
      font-weight: 600;
      color: #444;
    }

    .badge {
      margin: 3px 3px 3px 0;
    }

    .back-btn {
      background: #3f51b5;
      color: #fff;
      border: none;
      padding: 10px 20px;
      border-radius: 10px;
      text-decoration: none;
      transition: background-color 0.3s ease;
      display: inline-block;
      margin-bottom: 25px;
    }

    .back-btn:hover {
      background: #2c387e;
      color: #fff;
    }

    .carousel-control-prev-icon,
    .carousel-control-next-icon {
      background-color: rgba(0, 0, 0, 0.6);
      border-radius: 50%;
      width: 45px;
      height: 45px;
      background-size: 70% 70%;
    }

    .carousel-control-prev,
    .carousel-control-next {
      width: 5%;
    }
  </style>
</head>
<body>
  <div class="detail-wrapper">
<% if (product.isSold) { %>
  <a href="/admin/soldproducts" class="back-btn">
    <i class="fas fa-arrow-left me-2"></i> Back to Sold Products
  </a>
<% } else if (from === 'owned') { %>
  <a href="/admin/ownedproducts" class="back-btn">
    <i class="fas fa-arrow-left me-2"></i> Back to Owned Products
  </a>
<% } else if (from === 'notowned') { %>
  <a href="/admin/notownedproducts" class="back-btn">
    <i class="fas fa-arrow-left me-2"></i> Back to Dealer Products
  </a>
<% } else { %>
  <a href="/admin/adminproducts" class="back-btn">
    <i class="fas fa-arrow-left me-2"></i> Back to Manage Products
  </a>
<% } %>



    <h2 class="product-title"><%= product.title %></h2>

    <% if (product.pictures && product.pictures.length > 0) { %>
      <div id="productCarousel" class="carousel slide mb-4" data-bs-ride="carousel">
        <div class="carousel-indicators">
          <% product.pictures.forEach((_, index) => { %>
            <button type="button" data-bs-target="#productCarousel" data-bs-slide-to="<%= index %>" class="<%= index === 0 ? 'active' : '' %>"></button>
          <% }) %>
        </div>
        <div class="carousel-inner">
          <% product.pictures.forEach((pic, index) => { %>
            <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
              <img src="/uploads/<%= pic %>" class="d-block w-100 rounded" alt="Image <%= index + 1 %>">
            </div>
          <% }) %>
        </div>
        <% if (product.pictures.length > 1) { %>
          <button class="carousel-control-prev" type="button" data-bs-target="#productCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
          </button>
          <button class="carousel-control-next" type="button" data-bs-target="#productCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
          </button>
        <% } %>
      </div>
    <% } else { %>
      <div class="text-muted">No image available</div>
    <% } %>

    <div class="info-box"><span class="info-label">Brand:</span> <%= product.brand %></div>
    <div class="info-box"><span class="info-label">Model:</span> <%= product.model %></div>
    <div class="info-box"><span class="info-label">Year:</span> <%= product.year %></div>
    <div class="info-box"><span class="info-label">Price:</span> PKR <%= product.price.toLocaleString() %></div>
    <div class="info-box"><span class="info-label">Stock:</span> <%= product.stock %></div>
    <div class="info-box"><span class="info-label">Condition:</span> <%= product.condition %></div>
    <div class="info-box"><span class="info-label">Registration City:</span> <%= product.registrationCity %></div>
    <div class="info-box"><span class="info-label">Engine Type:</span> <%= product.engineType %></div>
    <div class="info-box"><span class="info-label">Engine Capacity:</span> <%= product.engineCapacity %></div>
    <div class="info-box"><span class="info-label">Transmission:</span> <%= product.transmission %></div>
    <div class="info-box"><span class="info-label">Owned By:</span> <%= product.ownedBy %></div>

    <% if (product.ownedBy === 'Allah Hoo Motors') { %>
      <div class="info-box"><span class="info-label">Purchase Cost:</span> PKR <%= product.cost?.toLocaleString() || 'N/A' %></div>
    <% } %>

    <div class="info-box">
      <span class="info-label">Features:</span>
      <% if (product.features && product.features.length > 0) { %>
        <% product.features.forEach(f => { %>
          <span class="badge bg-primary"><%= f %></span>
        <% }) %>
      <% } else { %>
        <span class="text-muted">No features listed</span>
      <% } %>
    </div>

    <div class="info-box">
      <span class="info-label">Description:</span>
      <p><%= product.description %></p>
    </div>

    <hr class="my-4">

<p><strong>Added By:</strong> <%= product.addedBy?.name || 'Unknown' %></p>
<% if (product.editedBy) { %>
  <p><strong>Last Edited By:</strong> <%= product.editedBy.name %></p>
<% } %>




<div class="info-box"><span class="info-label">Buy Date:</span> 
  <%= product.buyDate ? new Date(product.buyDate).toLocaleDateString() : 'N/A' %>
</div>

<div class="info-box"><span class="info-label">Sold Date:</span> 
  <%= product.soldDate ? new Date(product.soldDate).toLocaleDateString() : 'Not Sold Yet' %>
</div>

<%
  const created = new Date(product.createdAt);
  const createdFormatted = `${created.getDate()}-${created.getMonth()+1}-${created.getFullYear()}`;
%>
<p><strong>Created On:</strong> <%= createdFormatted %></p>

<p><strong>Last Updated:</strong> <%= product.updatedAt.toLocaleString() %></p>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
