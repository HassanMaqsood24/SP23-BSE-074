<!DOCTYPE html>
<html lang="en">
<head>
  <title>Set Commission</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="container mt-5">
  <h2>Set Commission for "<%= product.title %>"</h2>
  <form action="/admin/adminproducts/setcommission/<%= product._id %>" method="POST">
    <div class="mb-3">
      <label for="commissionPercent" class="form-label">Enter Commission Percentage (%)</label>
      <input type="number" class="form-control" id="commissionPercent" name="commissionPercent" required step="0.1" min="0" max="100">
    </div>
    <button type="submit" class="btn btn-success">Mark as Sold</button>
    <a href="/admin/adminproducts" class="btn btn-secondary">Cancel</a>
  </form>
</body>
</html>
