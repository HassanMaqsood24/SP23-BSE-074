<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= product.title %> • Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
  <style>
    body {
      background: #f4f4f4;
      padding: 40px 20px;
      font-family: 'Segoe UI', sans-serif;
    }

    .detail-wrapper {
      background: #ffffff;
      padding: 40px;
      max-width: 1000px;
      margin: auto;
      border-radius: 20px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
    }

    .product-title {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 20px;
    }

    .info-box {
      margin-bottom: 15px;
    }

    .info-label {
      font-weight: 600;
      color: #333;
    }

    .back-btn {
      background: #3f51b5;
      color: #fff;
      border: none;
      padding: 10px 20px;
      border-radius: 10px;
      text-decoration: none;
      transition: background-color 0.3s ease;
    }

    .back-btn:hover {
      background: #2c387e;
      color: #fff;
    }

    .badge {
      margin-right: 5px;
    }

.carousel-control-prev-icon,
.carousel-control-next-icon {
  background-color: rgba(0, 0, 0, 0.6);
  border-radius: 50%;
  width: 45px;
  height: 45px;
  background-size: 70% 70%;
}

.carousel-control-prev,
.carousel-control-next {
  width: 5%;
}

  </style>
</head>
<body>
 <div class="detail-wrapper">
<% if (product.isSold) { %>
  <a href="/user/usersoldproducts" class="back-btn">
    <i class="fas fa-arrow-left me-2"></i> Back to Sold Products
  </a>
<% } else if (from === 'wishlist') { %>
  <a href="/user/wishlistedproducts" class="back-btn">
    <i class="fas fa-arrow-left me-2"></i> Back to wishlisted Products
  </a>

<% } else { %>
  <a href="/user/userproducts" class="back-btn">
    <i class="fas fa-arrow-left me-2"></i> Back to Featured Products
  </a>
<% } %>



    <h2 class="product-title"><%= product.title %></h2>

    <% if (product.pictures && product.pictures.length > 0) { %>
      <div id="productCarousel" class="carousel slide mb-4" data-bs-ride="carousel">
        
        <!-- Carousel indicators -->
        <div class="carousel-indicators">
          <% product.pictures.forEach((_, index) => { %>
            <button type="button" data-bs-target="#productCarousel" data-bs-slide-to="<%= index %>" class="<%= index === 0 ? 'active' : '' %>" aria-current="<%= index === 0 ? 'true' : 'false' %>"></button>
          <% }) %>
        </div>

        <div class="carousel-inner">
          <% product.pictures.forEach((pic, index) => { %>
            <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
              <img src="/uploads/<%= pic %>" class="d-block w-100" alt="Product Image <%= index + 1 %>">
            </div>
          <% }) %>
        </div>

        <% if (product.pictures.length > 1) { %>
          <button class="carousel-control-prev" type="button" data-bs-target="#productCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
          </button>
          <button class="carousel-control-next" type="button" data-bs-target="#productCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
          </button>
        <% } %>
      </div>
    <% } else { %>
      <div class="text-muted">No image available</div>
    <% } %>

    <div class="info-box"><span class="info-label">Brand:</span> <%= product.brand %></div>
    <div class="info-box"><span class="info-label">Model:</span> <%= product.model %></div>
    <div class="info-box"><span class="info-label">Year:</span> <%= product.year %></div>
    <div class="info-box"><span class="info-label">Price:</span> PKR <%= product.price.toLocaleString() %></div>
    <div class="info-box"><span class="info-label">Stock:</span> <%= product.stock %></div>
    <div class="info-box"><span class="info-label">Condition:</span> <%= product.condition %></div>
    <div class="info-box"><span class="info-label">Registration City:</span> <%= product.registrationCity %></div>
    <div class="info-box"><span class="info-label">Engine Type:</span> <%= product.engineType %></div>
    <div class="info-box"><span class="info-label">Engine Capacity:</span> <%= product.engineCapacity %></div>
    <div class="info-box"><span class="info-label">Transmission:</span> <%= product.transmission %></div>

    <div class="info-box">
      <span class="info-label">Features:</span>
      <% if (product.features && product.features.length > 0) { %>
        <% product.features.forEach(f => { %>
          <span class="badge bg-primary"><%= f %></span>
        <% }) %>
      <% } else { %>
        <span class="text-muted">No features listed</span>
      <% } %>
    </div>

    <div class="info-box">
      <span class="info-label">Description:</span>
      <p><%= product.description %></p>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
